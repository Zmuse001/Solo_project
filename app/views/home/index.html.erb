<h1>Welcome To Feast N Fit</h1>
<%= stylesheet_link_tag 'Home' %>
<% if user_signed_in?%>
  <div> 🍔 
    Welcome, <%= current_user.username%>! </div>
  <%=button_to "Sign out", destroy_user_session_path, method: :delete%>

<% else %>
  <%= button_to "Sign in", new_user_session_path%>
<% end %>